import React from "react";
// import ImagesWrapper from "../../components/ImagesWrapper";

import { Container } from "./styles";

const Home: React.FC = () => {
  return (
    <Container>
      {/* {doesDataExists && <ImagesWrapper data={data} />}
      {isFetching && <span>Loading...</span>} */}
    </Container>
  );
};

export default Home;
